package dmuc

const (
	//USERBIN is the string constant for the /usr/bin/ directory
	USERBIN = "/usr/bin/"
	// USERLOCALBIN is the string constant for the /usr/local/bin directory
	USERLOCALBIN = "/usr/local/bin"
	// GREP  is the string constant for the 'grep' bash command
	GREP = "grep"
	// LS is the string constant for the 'ls' bash command
	LS = "ls"
)
